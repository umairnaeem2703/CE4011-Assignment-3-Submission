Act as a senior structural engineering software developer. I need to extend my 2D structural analysis Python engine (which currently supports frames, trusses, and thermal loads) to support **nodal support settlements**.

My codebase uses a strict Object-Oriented pipeline: `parser.py` -> `dof_optimizer.py` (RCM banded optimization) -> `matrix_assembly.py` -> `banded_solver.py` -> `post_processor.py`. 

You must implement support settlements using the matrix partitioning formulation:
* [K] is partitioned into free (f) and restrained (r) DOFs.
* The governing equation for active DOFs is: **K_ff * U_f = F_f - K_fr * U_r**
* The "unbalanced force vector" due to settlements is **-K_fr * U_r**.
* The support reactions are recovered via: **F_r = K_rf * U_f + K_rr * U_r**

Because my global K matrix is stored in a highly optimized banded format (`banded_solver.py`), DO NOT attempt to build dense global K_fr or K_rr matrices. Instead, process the settlement forces at the **element level** during assembly. 

Please execute the following implementation steps carefully:

1. **Update Schema & Input (data/SCHEMA.xml & test XMLs):**
   - Extend the `<support>` tag definition to accept optional float attributes for settlements: `settlement_ux`, `settlement_uy`, `settlement_rz`. Default to 0.0.
   - Update at least one test XML file to include a settlement (e.g., `settlement_uy="-0.002"` for a 2mm downward settlement).

2. **Update Data Models (`parser.py`):**
   - Modify the `Support` dataclass to store these float settlement values alongside the boolean restraint flags.
   - Update the parser logic to safely extract and cast these to floats.

3. **Update Element Physics & Matrix Assembly (`element_physics.py` & `matrix_assembly.py`):**
   - During `MatrixAssembler.assemble()`, when looping through elements, check if the element connects to a node with a non-zero settlement.
   - For an element, extract its local known displacement vector `{u_r}_e` for the restrained DOFs (with zeros for free DOFs).
   - Compute the element-level unbalanced force vector: `{f_unbalanced}_e = [k]_global * {u_r}_e`.
   - Subtract the active DOF components of `{f_unbalanced}_e` from the global active load vector `{F}`. This mathematically achieves `-K_fr * U_r` without needing a global $K_{fr}$ matrix.

4. **Update Post-Processing (`post_processor.py`):**
   - The full nodal displacement vector must now stitch together the solved active DOFs (`U_f`) AND the prescribed settlement DOFs (`U_r`).
   - When recovering member-end forces, ensure the element displacement vector includes the settlement values: `{f'}_e = [k']_e * {d'}_e + {FEF}_e`.
   - Ensure support reactions account for the settlements inherently by using the updated local member forces.

5. **Generate Tests:**
   - Write a unit test in `test_unit.py` verifying that an element subjected to a known 0.1m displacement at one end generates the correct internal shear/moments (e.g., $6EI\Delta/L^2$).
   - Write an interface/integration test proving the equivalent nodal loads are correctly subtracted from the global `{F}` vector.

Take your time and explain which files you are modifying before you output the code. Maintain my existing rule of NOT using NumPy/SciPy.

